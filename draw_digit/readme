1）使用ScrPlot（ ）函数调用，通过画点的方式，将字符‘5’在POS屏幕上显示出来。
 
已知， 字符‘5’的点阵信息（按从左到右、从上到下的次序）是："\x7C\x40\x78\x04\x04\x44\x38\x00"；//5
 
2）通过程序运算的方式，将字符‘5’顺时针旋转90度、画点显示到POS屏幕上。
 
3）将 ‘5’的原始点阵，横向和竖向均放大2倍，即：一个点会放大成正方形分布的4个点，显示到POS屏幕上。
